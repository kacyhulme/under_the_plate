<%= form_for(@establishment) do |f| %>
  <%= render "shared/errors", object: @establishment %>
  <fieldset>
    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: "form-control" %>
    </div>
   
    <div class="form-group">
      <%= f.label :address_id %>
      <%= collection_select :establishment, :address_id, Address.all, :id, :address_display_name, {prompt: "Select an address"}, {class: "form-control"} %>
    </div>

    <div class="form-group">
      <%= f.label :owner_id %>
      <%= collection_select :establishment, :owner_id, Owner.all, :id, :name, {prompt: "Select an owner"}, {class: "form-control"} %>
    </div>

    <div class="form-group">
      <%= f.label :distributor_id %>
      <%= collection_select :establishment, :distributor_id, Distributor.all, :id, :name, {prompt: "Select a distributor"}, {class: "form-control"}  %>
    </div>

    <div class="form-group">
      <%= f.label :cuisine %>    
      <%= f.text_field :cuisine, class: "form-control" %>
    </div>

    <div class="actions">
      <% if @establishment.new_record? %>
        <%= f.submit "Add Establishment", class: "btn btn-primary" %>
      <% else %>
        <%= f.submit "Update Establishment", class: "btn btn-primary"  %>
      <% end %>
    </div>
  </fieldset>
<% end %>